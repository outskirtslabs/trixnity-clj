= ol.trixnity.key

Encryption-key state, backup state, and trust-level queries.

## Upstream Mapping

This namespace maps to Trixnity's `KeyService` and `KeyBackupService`.

The public wrappers here cover:

- current and flow-based backup bootstrap state
- current and flow-based backup-version observation
- trust-level queries for users, devices, and timeline events
- device-key and cross-signing-key lookup

Use xref:api/ol-trixnity-verification.adoc[`ol.trixnity.verification`] for active verification workflows and
xref:api/ol-trixnity-room.adoc[`ol.trixnity.room`] when you need room timeline events to pair with
trust-level checks.

[#current-bootstrap-running]
== current-bootstrap-running

[source,clojure]
----
(current-bootstrap-running client)
----

Returns whether cross-signing bootstrap is currently running.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/key.clj#L27-L30[source,window=_blank]

'''

[#bootstrap-running]
== bootstrap-running

[source,clojure]
----
(bootstrap-running client)
----

Returns a Missionary flow of cross-signing bootstrap state.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/key.clj#L32-L36[source,window=_blank]

'''

[#current-backup-version]
== current-backup-version

[source,clojure]
----
(current-backup-version client)
----

Returns the current active room-key-backup version, or nil when unavailable.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/key.clj#L38-L41[source,window=_blank]

'''

[#backup-version]
== backup-version

[source,clojure]
----
(backup-version client)
----

Returns a Missionary flow of the active room-key-backup version.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/key.clj#L43-L47[source,window=_blank]

'''

[#get-trust-level]
== get-trust-level

[source,clojure]
----
(get-trust-level client user-id)
(get-trust-level client user-id-or-room-id device-id-or-event-id)
----

Returns a Missionary flow of trust information.

With two arguments, `user-id` resolves to the trust level of that user.

With three arguments, the meaning depends on the first id:

- a `@user:server` value resolves device trust for `device-id`
- a `!room:server` value resolves the trust level for the device that sent
  the timeline `event-id`


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/key.clj#L49-L71[source,window=_blank]

'''

[#get-device-keys]
== get-device-keys

[source,clojure]
----
(get-device-keys client user-id)
----

Returns a Missionary flow of device keys for `user-id`.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/key.clj#L73-L77[source,window=_blank]

'''

[#get-cross-signing-keys]
== get-cross-signing-keys

[source,clojure]
----
(get-cross-signing-keys client user-id)
----

Returns a Missionary flow of cross-signing keys for `user-id`.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/key.clj#L79-L83[source,window=_blank]
