= ol.trixnity.notification

Notification snapshots and notification update flows.

## Upstream Mapping

This namespace maps to Trixnity's `NotificationService`.

The public wrappers here cover:

- observing the full notification set in keyed or flat shapes
- looking up individual notifications and unread counts
- consuming live notification updates
- a deprecated bridge for notification extraction from sync responses

Use xref:api/ol-trixnity-room.adoc[`ol.trixnity.room`] for room timeline access and
xref:api/ol-trixnity-client.adoc[`ol.trixnity.client`] for client lifecycle.

[#get-notifications]
== get-notifications

[source,clojure]
----
(get-notifications client)
(get-notifications client response-or-opts)
(get-notifications client response opts)
----

Returns deprecated notification extraction flows.

Prefer <<get-all,`get-all`>>, <<get-by-id,`get-by-id`>>, and <<get-all-updates,`get-all-updates`>> for the current
notification model.

Supported opts:

| key                              | description                                                      |
|----------------------------------|------------------------------------------------------------------|
| `::mx/decryption-timeout`        | Decryption timeout for derived timeline events                   |
| `::mx/sync-response-buffer-size` | Number of sync responses buffered while extracting notifications |

_deprecated_

[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L26-L58[source,window=_blank]

'''

[#get-all]
== get-all

[source,clojure]
----
(get-all client)
----

Returns a Missionary flow of the current notifications as a list of inner flows.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L60-L63[source,window=_blank]

'''

[#get-all-flat]
== get-all-flat

[source,clojure]
----
(get-all-flat client)
----

Returns a Missionary flow of flattened notification snapshots.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L65-L68[source,window=_blank]

'''

[#get-by-id]
== get-by-id

[source,clojure]
----
(get-by-id client id)
----

Returns a Missionary flow of the notification with `id`, or nil when unavailable.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L70-L74[source,window=_blank]

'''

[#get-count]
== get-count

[source,clojure]
----
(get-count client)
(get-count client room-id)
----

Returns a Missionary flow of notification counts.

With one argument, returns the total count across all rooms.
With `room-id`, returns the count for that room.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L76-L85[source,window=_blank]

'''

[#is-unread]
== is-unread

[source,clojure]
----
(is-unread client room-id)
----

Returns a Missionary flow that is true when `room-id` is considered unread.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L87-L91[source,window=_blank]

'''

[#dismiss]
== dismiss

[source,clojure]
----
(dismiss client id)
----

Marks the notification with `id` as dismissed and returns a Missionary task.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L93-L97[source,window=_blank]

'''

[#dismiss-all]
== dismiss-all

[source,clojure]
----
(dismiss-all client)
----

Dismisses all notifications and returns a Missionary task.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L99-L102[source,window=_blank]

'''

[#get-all-updates]
== get-all-updates

[source,clojure]
----
(get-all-updates client)
----

Returns a Missionary flow of notification updates.

Upstream notes that this stream should not be buffered because consumed
updates are removed from the backing store as new values are requested.


[.api-source]
link:https://github.com/outskirtslabs/trixnity-clj/blob/main/src/clj/ol/trixnity/notification.clj#L104-L110[source,window=_blank]
